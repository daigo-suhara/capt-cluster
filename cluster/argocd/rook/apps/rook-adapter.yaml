apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-adapter
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  sources:
    - repoURL: https://opendev.org/openstack/openstack-helm
      targetRevision: master
      path: ceph-adapter-rook
      helm:
        releaseName: ceph-adapter-rook
        valueFiles:
          - $values/cluster/argocd/rook/values/rook-adapter.yaml
    - repoURL: https://github.com/daigo-suhara/capt-cluster.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: openstack
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
